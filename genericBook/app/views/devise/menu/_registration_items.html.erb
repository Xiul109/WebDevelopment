<% if user_signed_in? %>
  <%= button_to('Edit registration', edit_user_registration_path, style: "width:100%", class: 'btn btn-primary btn-sm', method: :get) %>
<% else %>
  <%= button_to('Register', new_user_registration_path, style: "width:100%", class: 'btn btn-primary btn-sm', method: :get)  %>
<% end %>

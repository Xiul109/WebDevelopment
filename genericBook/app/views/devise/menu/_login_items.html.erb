<% if user_signed_in? %>
  <%= button_to('Logout', destroy_user_session_path, style: "width:100%",class: 'btn btn-primary btn-sm', :method => :delete) %>        
<% else %>
  <%= button_to('Login', new_user_session_path, style: "width:100%",class: 'btn btn-primary btn-sm', method: :get)  %>  
<% end %>
